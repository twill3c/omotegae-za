# SPEC.md — omotegae-za(面替え座)

<!-- scaffold template v1.24.0 から展開(2026-09-01)。以後このファイルはプロジェクトが育てる -->

ギリシア四大劇作家の現存全劇を、**上演の骨格**として読み直す。
あわせて、原文の行番号のまま和訳を段階的に充填する。

本書の断定形は HC-077 の三区分に従う。**実測**は値と実測日を、**見込み**は確かめる手段を、
**実装の説明**は実物に当てて確かめたことを、それぞれ明示する。

---

## 1. 目的

ギリシア悲劇・喜劇には「語る俳優は最大 3 人」という上演上の規約があったとされる。
俳優は面を替えて複数の役を兼ねた。**この規約が、現存する台本そのものから復元できるか**を
45 篇すべてで測る。

同時に、45 篇の原文を**行番号を保ったまま**読める場所を作り、和訳を段階的に充填する。

分析と和訳は独立に進む。**分析は 45/45 篇で完成させ、和訳ゼロでも出荷できる**構成にする。

---

## 2. 素材(実測 2026-09-02)

出典は `PerseusDL/canonical-greekLit`(GitHub)のギリシア語 TEI XML。
採録した 45 ファイルは `data/raw/` に原本のまま置く。台帳は `data/derived/inventory.json`。

| 作家 | 篇数 | 行(`<l>`) | 発話(`<sp>`) | antilabe(`@part`) | 校訂者 |
|---|---:|---:|---:|---:|---|
| アイスキュロス | 7 | 8,081 | 1,509 | **2** | Herbert Weir Smyth(1922/1926) |
| ソポクレス | 8 | 10,529 | 2,818 | 324 | Francis Storr(1912/1913)+ Hunt(断片 1 篇) |
| エウリピデス | 19 | 26,456 | 6,339 | 709 | Gilbert Murray(1902/1913) |
| アリストパネス | 11 | 17,031 | 6,624 | **3,468** | F. W. Hall / W. M. Geldart(1906/1907) |
| **計** | **45** | **62,097** | **17,290** | **4,503** | |

ソポクレス 8 篇は現存 7 篇 +『追跡者たち』(断片サテュロス劇・405 行)。
エウリピデス 19 篇は現存全篇(『イオン』『嘆願する女たち』を含む)。

### 2.1 **校訂者が作家と一対一で対応している(最重要の交絡)**

上表のとおり、**作家ごとに校訂者が一人**である。したがって
**マークアップから出てくる作家間の差は、校訂者間の差と原理的に分離できない。**

antilabe の 2 対 3,468 という差は、アイスキュロスがそれを避けたことの証拠には**ならない**。
Smyth が行分割を `@part` で刻まなかっただけの可能性がある。

- この交絡を受けるもの: antilabe 率、行番号の枝番(`147a` 等)の付け方、話者名の表記法
- 交絡が比較的弱いもの: **話者の系列そのもの**(写本の伝統に強く縛られる)、
  **合唱隊の発話位置**(劇の構造に属する)

**規範:** 作家間の比較を主張するときは、その量が校訂実務で動きうるかを毎回書く。
動きうるなら主張にしない。これは G-06 で機械的に検査する。

### 2.2 素材の癖(実測 2026-09-02・全数走査)

| # | 実測 | 下流への影響 |
|---|---|---|
| ① | 話者マークアップは 2 流儀。`<speaker>本文</speaker>` が 44 篇、`<speaker n="#ID"/>` が 1 篇(『蛙』) | 読み取り層で吸収する。『蛙』の ID 形式は名寄せ済みで、むしろ良質 |
| ② | **共同発話が 2 篇 5 箇所。**『騎士』に `Δημοσθένης καὶ Νικίας` `Κλέων καὶ Ἀλλαντοπώλης`、『蛙』に `Αἰσχύλος καὶ Εὐριπίδης` ×3 | **「一発話 = 一話者」の前提が崩れる。** 彩色の頂点をどう置くかに直結する |
| ③ | **`<speaker>` を持たない `<sp>` が 3 件。**『追跡者たち』203 行 / 『ペルシア人』200 行 /『アガメムノン』83 行 | 3 件とも直前話者の続き。行番号の連続(202→203, 199→200, 82→83)を**個別に目視確認済み**。素通しで読むと話者列が 3 箇所欠ける |
| ④ | **行番号は密な整数列ではない。** 全 45 篇に枝番(`147a` `1017c` 等)がある。『鳥』251 種、『蛙』287 種 | 「n の最大値 = 行数」は成り立たない。突合オラクルの設計を変える(G-02) |
| ⑤ | **底本に誤植 1 件。**『福の神』に `n="3933"`。前後は `393 / 393b / 393c / 393d / [3933] / 393f / 394` で、`393e` の誤植 | `data/derived` 側に補正表を持つ。`data/raw` は原本のまま据え置く。上流(Perseus Wiki)への報告を検討 |

②③⑤ は HC-120 として起票済み。

### 2.3 権利

Perseus canonical-greekLit は README で **CC BY-SA 4.0**(別段の表示がない限り)と定める。
本プロジェクトはこれを受け入れる。

- **コード = MIT / `data/` と和訳 = CC BY-SA 4.0** の二本立て
- 各篇のページに **Perseus Digital Library(Tufts)+ 底本(校訂者・刊年)+ CC BY-SA 4.0** を表示
- 和訳は Perseus のギリシア語校訂本文に基づく二次的著作物として、同じく CC BY-SA 4.0 で提供する
- 底本情報は TEI ヘッダの `sourceDesc` から機械的に取れる(実測 2026-09-02・45/45 篇で取得)

---

## 3. 目玉 —— 三人に収まらない役を特定する

> **改訂(L4・2026-09-02)。** 当初の目玉は「三人で上演できることを示す」だった。
> §3.5 のとおり **G-07 は厳・緩の両方で落ちた**。§3.2 に書いた手順に従って格下げする。
> ただし格下げ先は「何も言えなかった」ではない。L4 の対照(§3.6)により、
> **χ の小ささが役数の産物ではないことが示された**ため、目玉はこう置き直す:
>
> **45 篇それぞれについて χ を厳密に求め、χ ≥ 4 になる篇では「三人に収まらない役」を
> 名指しする。** 三人性が成り立つ範囲と、成り立たない箇所の正体を、同じ物差しで出す。
>
> 以下 §3.1〜§3.5 は**当初の目玉と、それが落ちた経緯の記録**である。消さずに残す。

### 旧・目玉 —— 三人の俳優で上演できるか

篇ごとに次のグラフを作り、**彩色数 χ を上限を置かずに厳密に求める**。

- 頂点 = 語る役(合唱隊を除く)
- 辺 = 「同じ場面に居合わせるので同一俳優が兼ねられない」

### 3.1 事前登録する予測(2026-09-02 に登録)

| 群 | 予測 |
|---|---|
| アイスキュロス初期(ペルシア人・七将・嘆願する女たち) | χ ≤ 2 |
| ソポクレス 7 + エウリピデス 19 + アイスキュロス後期 | χ ≤ 3 |
| アリストパネス 11 | χ > 3 の篇が出る |
| レソス(偽作説)・追跡者たち(断片) | 周囲から外れるか(横目で見るだけ。主張にはしない) |

### 3.2 落ちる条件(先に書く)

χ が予測とずれたとき、**まず疑うのは自分の場面境界の代理指標であって、通説ではない。**

1. 厳・緩の二通りで測り直す(§3.3)
2. それでも合わなければ、**代理指標のほうを捨てる**
3. 三度直して合わなければ、目玉を
   **「校訂本文の話者ラベルだけからは三人性を復元できない」**という否定的結論に格下げして出荷する

格下げは失敗ではない。umi-no-ki の G-目玉1 と同じ扱いをする。

### 3.3 場面境界の代理指標

本文に退場は書かれていない(ト書きは近代の編者による補い)。合唱隊の歌が登退場を許すという
上演上の性質を使って、二通りの境界を置く。

- **厳** — **合唱隊だけで構成される `div`** でのみ切る(場面は大きく、衝突は増え、χ は大きく出る)
- **緩** — 合唱隊の任意の発話で切る(場面は細かく、衝突は減り、χ は小さく出る)

**厳が反証側である。**「χ ≤ 3」を主張したいなら厳で通らねばならない。
**結論が両方で一致したときだけ主張する。** 片方でしか成り立たない篇は、そう書いて別扱いにする。

#### 閾値を置かなかった理由(L2 実測 2026-09-02)

当初は「一定行数以上の合唱隊単独発話」を厳の境界にする予定だった。**やめた。**

- 合唱隊 2,763 発話の長さの分布に**谷が無い**(1 行 747 / 2 行 512 / 3 行 240 と単調減衰、
  中央値 3・平均 5.2)。合唱隊長の短い受けと本格的な合唱歌を分ける自然な閾値をデータが与えない
- 閾値を自分で決めれば、その値は結果を見て動かせる(G-04 違反)
- `div/@subtype` の語彙で決めることも検討したが、**56 種あり校訂者ごとにばらける**
  (`episode`/`Episode`、`choral`/`Choral`、`epirrheme`/`Epirrheme`/`epirrhema`)。
  `@rend` に至っては アイスキュロス 0.1% / ソポクレス 0.0% / エウリピデス 25.5% /
  アリストパネス 11.8% で、韻文種別の手がかりには使えない(§2.1 の交絡)

そこで**語彙を判定に使わない**。`div` の境界だけを編者から借り、
「その `div` で語るのが合唱隊だけか」は L1 の話者分類から決める。

**非循環の裏づけ(実測 2026-09-02):** 語彙を一切使っていないにもかかわらず、
厳で境界に選ばれた `div` は `strophe` 372 / `antistrophe` 367 / `lyric` 51 /
`epode` 38 / `ephymn` 系 27 と、合唱歌の語彙に集中した。判定が合唱歌を拾えている証拠になる。

#### 代理指標の既知の弱点(L2 実測 2026-09-02)

**合唱隊が語らない区間では、退場を検出できない。** とくに合唱隊登場前の
プロロゴスは丸ごと一場面に併合される。実例:

- 『エウメニデス』緩で 4 役同席(ピュティア・アポロン・オレステス・クリュタイムネストラの亡霊)。
  実際にはピュティアは亡霊の登場前に退場しており、**同席していない**
- 対して『嘆願する女たち』2・『アガメムノン』緩 2 は通説と合う

この弱点は §3.2 の手順 2「代理指標のほうを疑う」に直結する。L3・L4 で χ を出したあと、
食い違いがこの弱点で説明できるかを篇ごとに確かめる。

### 3.4 決着しない話者ラベルの扱い(L1 実測 2026-09-02)

45 篇 497 組の話者ラベルを全数目視した結果、**本文からは決着しないものが 4 組**残った。

| 篇 | ラベル | 争点 |
|---|---|---|
| アカルナイの人々 | `Κόρα` / `Κόρη` | メガラ人の娘は 2 人いる(784「この子」/ 789「もう一方」)が、校訂者はドーリス形 `Κόρα` とアッティカ形 `Κόρη` を娘 1・娘 2 に対応させていない。803 で「そちらの子は?」と別の娘に問うた返事も `Κόρα` である。**1 頂点にすれば娘 2 人を潰し、2 頂点にすれば分割が娘の別と対応しない** |
| 女だけの祭 | `Χορὸς Ἀγάθωνος` | 104-129 の劇中劇の合唱(主合唱隊 312-1231 とは離れている)。上演ではアガトンを演じる俳優が独りで歌うと解されることが多い |
| 嘆願する女たち | `Δαναΐς` | 1052-1060。終曲の話者配分は校訂上の論争点。ダナオスの娘個人(俳優)か、ダナイデス合唱隊の一部かが本文から決まらない |

これらは**古典文献学の未決問題であって、実装の不備ではない。**
本プロジェクトが裁定すべきものでもない。

**したがって §3.3 の厳・緩と同じ扱いにする —— 両方の読みで χ を出し、併記する。**
読みによって χ が動く篇は、動いたことを図表に明記する。動かない篇は、
「この論争は結論に影響しない」と書ける(それ自体が有用な結果である)。

### 3.5 目玉の判定(L3 実測 2026-09-02)—— **G-07 は不通過**

45 篇 × 読み 2 × 厳緩 2 = 180 組の χ を厳密に求めた。Python(k を下限から上げる
DSATUR 後戻り)と TypeScript(集合分割を直接最小化)の二実装で **180/180 一致**(G-03)。
**悲劇 34 篇は全数が総当たり(集合分割の全列挙)でも裏づけられている。**

事前登録した G-07(悲劇 34 篇のうち χ ≤ 3 が 31 篇以上)の結果:

| 読み・境界 | χ ≤ 3 の悲劇 | 判定 |
|---|---:|---|
| 厳 | 18/34 | **不通過** |
| 緩 | 28/34 | **不通過** |

**通らなかった。** 事前に書いたとおり、以下を記録して次へ進む。

#### それでも出た構造(緩)

| 作家 | χ の分布 |
|---|---|
| アイスキュロス | 2, 2, 2, 3, 3, 4, 4 |
| ソポクレス | 2, 3, 3, 3, 3, 3, 4, 4 |
| **エウリピデス** | **3 が 17 篇**, 4, 4 |
| アリストパネス | 4, 5, 6, 6, 6, 6, 7, 8, 9, 9, 11 |

- **アリストパネス 11/11 が χ > 3。** 事前登録の予測どおり
- **エウリピデス 19 篇中 17 篇がちょうど χ = 3。** 三俳優の規約と極めてよく合う
- **アイスキュロスの 3 篇がちょうど χ = 2。** 二俳優期の通説と合う

#### χ ≥ 4 になった悲劇 6 篇 —— **6 篇すべてで超過は一つの 4-クリークに帰着する**

> **訂正(L4・2026-09-02)。** L3 の時点で「四人目は単独役で、供養する女たちではピュラデス」と
> 書いた。**誤りである。** それは厳密彩色が返した**一つの**最適解の色分けを読んだだけで、
> 最適彩色は複数あり、どれを「四人目」と呼ぶかは恣意的だった。
> 不変なのは彩色の中身ではなく、**互いに兼ねられない四役の組**である。
> 最小頂点削除(§4 F-14)で測り直したところ、6 篇すべてで**最小 1 役・候補 4 通り**、
> かつその 4 役は**クリークを成す**。どれを外しても χ は 3 に落ちる。

| 篇 | 兼ねられない四役 | 同席の場所 |
|---|---|---|
| 供養する女たち | クリュタイムネストラ / 召使 / **ピュラデス** / オレステス | 一場面(第 53) |
| エウメニデス | クリュタイムネストラの亡霊 / ピュティア / アポロン / オレステス | **一場面(第 0 = プロロゴス)** |
| ピロクテテス | ネオプトレモス / ピロクテテス / ヘラクレス / オデュッセウス | 一場面(第 34) |
| レソス | ディオメデス / アテナ / アレクサンドロス / オデュッセウス | 一場面(第 24) |
| ヘレネ | テオクリュメノス / メネラオス / 伝令 / ヘレネ | 複数場面から合成 |
| コロノスのオイディプス | テセウス / クレオン / オイディプス / アンティゴネ | 複数場面から合成 |

**4 篇では四役が一つの場面に同席し、2 篇では複数場面にまたがる組として現れる。**

供養する女たちの四役は、オレステス・ピュラデス・クリュタイムネストラ・召使 が
居合わせる場面である。**ピュラデスが 900-902 の三行しか語らないことは、
三俳優の規約をめぐって古典学が長く論じてきた当の箇所**であり、
この方法はその場面を機械的に切り出した —— ただし「ピュラデスが四人目だ」とは言えない。
四役のうち誰を外しても等しく解消する。

エウメニデスの四役が**プロロゴス(第 0 場面)**に現れることは、
§3.3 の既知の弱点(合唱隊が語らない区間で退場を検出できない)と一致する。
実際ピュティアは亡霊の登場前に退場しており、同席していない。

#### 決着しない 4 組は、ほとんど結論に影響しない(実測)

読み A と読み B で χ が動いたのは **『女だけの祭』のみ**(厳 8→7 / 緩 6→4)。
アカルナイの `Κόρα`/`Κόρη` も嘆願する女たちの `Δαναΐς` も χ を動かさなかった。
**§3.4 の論争のうち二つは結論に影響しない**と実測で言える。

### 3.6 置換対照 —— χ の小ささは役数の産物ではない(L4 実測 2026-09-02)

「χ ≤ 3 の悲劇が多い」のは、単にその篇の**役数が少ない**からではないか。
役が 5 つしかなければ χ は高々 5 で、3 に収まっても不思議はない。
これを排除しなければ目玉は何も主張していない。

**偽の劇の作り方:** 発話の並びと場面の境界はそのままに、**俳優発話に貼られた
役のラベルだけを篇内で無作為に置換する**(seed 20260902・2,000 回)。
置換は発話総数・場面の数と大きさ・境界の位置・異なり役数・各役の発話回数の
多重集合をすべて保ち、**どの役がどの場面に集まるか**だけを壊す。

#### 結果

| 境界 | 実測で χ ≤ 3 の悲劇 | 帰無で χ ≤ 3 が一度でも起きた篇 |
|---|---:|---:|
| 厳 | **18/34** | 2/34 |
| 緩 | **28/34** | 5/34 |

篇ごとの片側 p 値(補正 (k+1)/(N+1))は **厳 30/34・緩 29/34 の悲劇で p < 0.01**。
効果は大きく、たとえば『アンティゴネ』は実測 3 に対し帰無平均 8.06、
『フェニキアの女たち』は実測 3 に対し帰無平均 10.0 である。

**χ の小ささは役数でも発話数でも場面数でもなく、役の配置に由来する。**

#### 有意にならなかった 5 篇は、すべて理由が説明できる

| 篇 | 事情 |
|---|---|
| キュクロプス / 追跡者たち | 役が 3 つしかなく、帰無分布が一点に潰れる(置換しても何も動かない) |
| ピロクテテス | 役 5 つ・帰無の範囲が [4,5] で、動く余地がほとんど無い |
| 供養する女たち | 実測 4 は帰無の**最小値**そのもの(範囲 [4,7])。床に張り付いているが p = 0.037 |
| **エウメニデス** | **実測 4 が帰無平均 3.72 を上回る** |

#### 対照が独立に方法の弱点を名指しした

45 篇 × 厳緩 = 90 組のうち、**実測が帰無平均を上回ったのは『エウメニデス』緩の 1 組だけ**である。
これは §3.3 で「合唱隊が語らない区間では退場を検出できない」と指摘した篇と一致する。
実劇の構造が偶然より**悪く**見えるのは、プロロゴスが一場面に併合されて
同席していない役(ピュティアと亡霊)が結ばれるからである。

**弱点の指摘と対照は独立の経路であり、それが同じ 1 篇を指した。**

---

## 4. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 45 篇の TEI を読み、話者列・行・場面を `data/derived` に正規化する。2 流儀の話者マークアップ・共同発話・話者欠落・行番号枝番をすべて吸収する | must |
| F-02 | 話者名の名寄せ台帳(`data/derived/speakers.json`)を作る。**自動正規化はしない**。候補を出し、全数を目視して採否を記録する | must |
| F-03 | 場面分割を厳・緩の二通りで行う | must |
| F-04 | 衝突グラフを作り、χ を**厳密解**で求める(近似・貪欲を使わない) | must |
| F-05 | 彩色を Python と TypeScript で独立に実装し、45/45 で一致を確認する | must |
| F-06 | 置換対照(話者ラベルを乱数で置換した偽の劇)で χ の帰無分布を出す | must |
| F-07 | **骨格帯** — 45 篇を同幅に正規化した帯。話者を色、合唱隊を別レーン | must |
| F-08 | **配役盤** — 篇ごとの衝突グラフ・彩色・面替えスケジュール | must |
| F-09 | **登壇圏** — 同じ場面で語る者どうしのネットワーク | should |
| F-10 | **原文リーダー** — 45 篇を行番号のまま読む。話者に色 | must |
| F-11 | 英訳併記(Perseus に存在する **36 篇**。アリストパネスは 11 篇中 2 篇のみ) | should |
| F-12 | **和訳リーダー** — 原文と行対応で併記。充填率を篇ごとに表示する | must |
| F-13 | 各篇に Perseus・底本・CC BY-SA 4.0 を表示する | must |
| F-14 | **超過役の特定** — χ > k の篇で、G から取り除けば k 彩色になる最小の役集合を厳密に求め、**最小解を全て**返す。特定の一役を「四人目」と呼ばない | must |

**F-11 について(実測 2026-09-02・L6 で数え直した):** Perseus の英訳は **45 篇中 36 篇**
(ファイル 38 件 —— アガメムノンとレソスに二訳ある)。アリストパネスは 11 篇中 2 篇
(『雲』『鳥』)しかない。**欠けている 9 篇は空欄で出す。**

> **訂正。** L0 で「39/45」、L3〜L5 で「41 篇」と書いたが、どちらも誤り。
> GitHub の一覧を目で数えたまま検算しなかった。実測は 36 篇である。
Gutenberg での補完は本プロジェクトのスコープ外(§7)。

---

## 5. 和訳

### 5.1 規模と段階

**訳出対象は 61,968 行**(`<l>` 要素 62,097 のうち、本文が空の 121 行と
`n="0"` の幽霊行を除いた実数。実測 2026-09-02)。
ikari-uta(イリアス 15,687 行 / 25 ループ)の約 4 倍にあたる。
**一息でやらない。** 期ごとに出荷する。

| 期 | 対象 | 行数 | 当初見込み | 実測レートでの見込み |
|---|---|---:|---:|---:|
| 第一期 | アイスキュロス 7 | 8,081 | 16 | 20 |
| 第二期 | ソポクレス 8 | 10,529 | 20 | 26 |
| 第三期 | エウリピデス 19 | 26,456 | 50 | 65 |
| 第四期 | アリストパネス 11 | 17,031 | 35 | 42 |
| 計 | | 61,968 | 121 | **153** |

#### 見込みを引き直した(L14 実測 2026-09-03)

当初の見込みは ikari-uta / uta-gaeshi の実績レート(1 ループ 450〜600 行)に
基づく**見込みであって実測ではない**と明記していた。L9〜L13 の 5 ループを
実測して引き直す(L8 は検査の機械立てに費やしたので母数から外す)。

| ループ | 訳出行数 |
|---|---:|
| L9 | 339 |
| L10 | 633 |
| L11 | 302 |
| L12 | 301 |
| L13 | 469 |

**5 ループで 2,044 行、平均 409 行/ループ。** 当初見込みの下限 450 を下回る。

遅れの主因は**固有名の密度**である。台帳は「出現箇所を全部見てから入れる」
規律で作るので(SPEC §5.2 T-04、[[fleet-cyrillic-leak]] 型の事故を避けるため)、
新しい名が多い箇所ほど確認に時間がかかる。ペルシア人の将たちの一覧や
嘆願する女たちの冒頭は 1 ループで 20〜30 件の新規見出しを要した。
逆に対話が続く箇所は速い(L10 は 633 行)。

**この見込みも見込みである。** 台帳が育つほど新規見出しは減るはずなので、
第二期以降は上振れしうる。第一期の完了時にもう一度引き直す。

#### 三篇目を訳し終えて(L16 実測 2026-09-03)

| ループ | 訳出行数 |
|---|---:|
| L14 | 264 |
| L15 | 424 |
| L16 | 408 |

**8 ループで 3,140 行、平均 393 行/ループ。** L14 の引き直し(409)からさらに下がった。
上振れは起きていない。**台帳が育っても新規見出しは減らなかった** ——
『縛られたプロメテウス』の後半はイオの放浪の地誌そのもので、
カリュベス・ヒュブリステス・テミスキュラ・サルミュデソス・キステネ・アリマスポイと、
**一箇所しか出てこない地名**が続けて 17 件出た。台帳が効くのは繰り返し出る名だけで、
一回きりの名は何度目のループでも同じ手間がかかる。

数詞の見出しも同じで、L16 だけで τρεῖς / δεκ / πεμπτ / πεντ / δισσ / διπλ / διδυμ の
7 件を実測して足した。**見出しの取りこぼしは黙って検出を落とす**ので、
出てくるたびに測って足すほかない(τρεῖς は τρι で始まらないので素通りしていた)。

見込みは据え置く(153 ループ)。393 で引き直せば 158 になるが、
差は誤差の範囲で、母数 8 ループは引き直しの根拠として薄い。

第四期を最後に置くのは、地口・猥雑語・時事諷刺・悲劇のパロディが桁違いに難しいため。

### 5.2 和訳のオラクル

| ID | 検査 | 性質 |
|---|---|---|
| T-01 | 原文 `<l>` 一行 ↔ 訳文一行。消化率 1.000・欠番 0 | ikari-uta の背骨 |
| T-02 | 訳文の話者列が原文の `<sp>` 列と完全一致(17,290 発話) | 機械的・全数 |
| T-03 | **antilabe(`@part`)の位置で訳文も話者が交替しているか** | 意味を経由せず構造だけを見る。ただし §2.1 の交絡により**篇による疎密は校訂者の産物**でありうる |
| T-04 | 同じギリシア語名 → 同じカタカナ、45 篇横断で一貫。**両向き**に見る —— 一つのカタカナに複数の語幹(λ/ρ の潰れ)と、同じ語幹が二つのカタカナ(台帳への重複登録)| umi-no-ki の教訓(カタカナは λ/ρ を潰す)→ 衝突組は原語併記。逆向きは L15 で素通りしたので足した |
| T-05 | **原文語彙の消化率** — 固有名・数詞・呼びかけが原文側から見て落ちていないか | uta-gaeshi G-14/G-15:「訳文に何が入っているか」の検査は「原文の何が落ちたか」を捕まえない |
| T-06 | Perseus の英訳 36 篇と行単位で突き合わせ、食い違いを目視候補として出す | **検出器であってゲートではない**(英訳も一つの解釈にすぎない) |

T-05 は訳し終えてから走る**事後**の検査である。同じ形の失敗(原文の行にある固有名を、
訳文では隣の行に置く)が L9〜L15 で七度出たので、**同じ台帳を訳す前に引く**経路を足した。

```
python pipeline/check_translation.py demands <play> <lo> <hi>
```

どの行がどのカタカナ・どの数を要求するかを、台帳と数詞表からそのまま出す。
**事後の T-05 は一切緩めない** —— 前に置いた一覧が検査の代わりになってはならないので、
両者が同じ台帳を引いていることを対照で固定してある(一覧が要求した語を訳文から抜くと、
その行で T-05 が発火する)。詳細は HC-145。

**L17 で順序を変えた。** L16 まで「訳す → demands で確かめる」の順で使っていたが、
それでは新しい固有名は台帳に無いので一覧に出ず、二度ずらした。L17 からは

  (1) その篇の**大文字語を全数走査**して未登録の表層形を出す
  (2) 全数の出現箇所を見て台帳に入れる(既存の見出し・カタカナとの衝突は assert で止める)
  (3) `demands` を引く
  (4) 一覧を行番号ごとに突き合わせながら訳す

の順にした。**L17 は 249 行で隣行流出ゼロ。** 一覧が空欄でないことが効いている。

### 5.3 訳し分けと循環の禁止

対話部と合唱歌を訳し分ける(対話 = 常体の会話体 / 歌 = 文語調)のは**方針**であって主張ではない。
自分でそう訳したものを測って「歌は文語だった」と言うのは循環する。

**規律:** 訳すときに原文の `@rend` 等の詩形マークアップを見せない。訳し終えてから、
文体を切り替えた位置と原文側の境界が一致するかを照合し、**ずれた箇所を全部挙げる。**

---

## 6. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | cron・サーバ関数を持たない。課金経路ゼロ | `vercel.json` と関数ディレクトリの不在をテストで確認 |
| N-02 | 和訳が 0% でも分析アプリとして完結する | L7 で和訳ゼロの状態で出荷検品を通す |
| N-03 | 日本語本文に他字種が混入しない | `harness/text_hygiene.py`(キリル文字等)+ 和訳はギリシア文字の意図的併記のみ許可 |
| N-04 | `data/raw` は原本を改変しない | ハッシュを記録し、変更があればテストで落とす |

---

## 7. 品質ゲート

| ID | 内容 |
|---|---|
| **G-00** | **権利** — 45/45 篇で底本・校訂者・Perseus 帰属・CC BY-SA 4.0 の表示が揃っている。欠けたまま出荷しない |
| **G-01** | 45/45 篇で話者列を復元し、`<speaker>` 欠落 3 件の継承根拠が個別に記録されている。台帳の 497 組すべてに分類と**その根拠**が付いている。`review` に残るものは §3.4 に列挙され、**両方の読みで χ を出している**(未記載の `review` を 0 にする) |
| **G-02** | 場面分割の消化率 = 1.000・未割当 0。**行番号は枝番を含むので「n 最大 = 行数」を検査に使わない。** 代わりに (a) 枝番を含む n の重複が 0、(b) 数値部が単調非減少、(c) 誤植補正表に載っていない外れ値が 0 を検査する |
| **G-03** | 彩色の Python / TypeScript 二実装一致 45/45 |
| **G-04** | **循環の禁止** — χ の計算経路のどこにも定数 3 を入れない |
| **G-05** | 置換対照(seed 20260902・2,000 回)で実劇の χ が有意に小さい。**通過(L4 実測 2026-09-02: 厳 30/34・緩 29/34 の悲劇で p < 0.01)。** 有意にならなかった篇は §3.6 に理由を列挙する。**実測が帰無平均を上回った篇は必ず名指しし、方法の弱点として説明する**(現在 1 件: エウメニデス緩) |
| **G-06** | **交絡の表示** — 作家間比較を行う全ての図表に、その量が校訂実務で動きうるかの注記がある。注記のない作家間比較を 0 件にする |
| ~~G-07~~ | ~~悲劇 34 篇のうち χ ≤ 3 が 31 篇以上~~ → **不通過(厳 18/34・緩 28/34)。§3.5 に記録し、G-07′ に置き換えた** |
| **G-07′** | **目玉(改訂)** — (a) 45 篇の χ を厳・緩の両方で厳密に出す、(b) χ ≥ 4 の篇では **「三人に収まらない役」を名指しする**、(c) 群水準の対照で実測の χ ≤ 3 篇数が帰無を上回ることを示す、(d) 実測が帰無平均を上回る篇を全て挙げて弱点として説明する。**(a)〜(d) のいずれかを落としたまま出荷しない** |
| **G-08** | 字種検査(N-03) |
| **G-09** | 図と数の一致(satei-kobo HC-001) |
| **G-10** | 実ブラウザ検品 |
| **G-11** | 和訳の T-01〜T-05 が、訳出済みの範囲で全数通過 |

---

## 8. スコープ外

- **Project Gutenberg は使わない。** 素材は Perseus 一本。英訳の欠けは空欄で出す
- **韻律・韻の再現** — 訳では移せない(ikari-uta / uta-gaeshi の既知)
- **作家間の語彙比較** — §2.1 の交絡により、校訂者を測ることになる
- **ラテン語劇(セネカ等)・断片作家** — 対象は四大劇作家の Perseus 収録 45 篇のみ
- **底本の校訂そのものへの介入** — 誤植は補正表に記録するが、`data/raw` は書き換えない
